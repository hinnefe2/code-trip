---
name: archive-github-bot-notification
description: Archive GitHub notification emails (sender notifications@github.com) that report ONLY routine automated bot activity — CI runs that PASSED, Dependabot/Renovate dependency PRs and alerts, automated security advisories, automated labeling/assignment changes, comments authored by GitHub Apps. Pick this skill when the entire body of the email is uneventful bot activity with no human involvement AND no failure signal. SKIP (so the user sees it) when EITHER (a) the email involves any human interaction with the user's work — review requests, review comments, replies on the user's PRs or issues, @-mentions by a human, a human assigning the user, a human merging or closing the user's PR — OR (b) the email reports a CI failure, a workflow that did not succeed, a failed status check, a failed deploy, or any other error signal the user should see. When unsure whether a comment author is a human or a bot, treat them as human (forward to the user). When unsure whether a CI run passed or failed, forward.
auto-handle: true
auto-handle-kinds:
  - email_msg
allowed-tools:
  - mcp__claude_ai_Gmail__unlabel_thread
---

# Archive bot-only GitHub notification

You're being invoked on an email task that looks like a GitHub notification reporting purely automated activity. Archive it from the user's inbox.

1. **Sanity-check the body.** Confirm the body describes only routine, uneventful bot/automation activity:
   - CI runs that PASSED / succeeded / completed cleanly (GitHub Actions, CircleCI, etc.)
   - Dependabot, Renovate, or other dependency-update bots
   - Security alerts auto-generated by GitHub
   - Automated label / assignee / status changes by a GitHub App
   - Comments authored by a clearly-named bot account (suffix `-bot`, `[bot]`, or a well-known automation account)

   STOP and do not archive if you see ANY of:
   - A human review request, a human review comment, a human approving/requesting changes, a human reply on a PR or issue thread, a human @-mention of the user. Return: `Not archived — this involves human activity.`
   - A CI failure, a failed workflow run, a failed status check, a failed deploy, or any other error signal — the user wants to see CI failures even though they're bot-emitted. Return: `Not archived — this reports a CI failure.`

2. **Archive the email.** Use `mcp__claude_ai_Gmail__unlabel_thread` with the email's `thread_id` (from the task source) and `labelIds=["INBOX"]`.

Don't ask for confirmation. When archived, return ONE sentence: `Archived GitHub bot notification: <brief subject>.`

This skill runs in two modes:
- **ACT+PTT (voice):** the user is holding the active task and explicitly asked to archive it. Their intent is the trigger.
- **Auto-handle (screener):** no user instruction; you're invoked because the screener classifier picked this skill. Apply the same body-check before archiving — when in doubt, leave it.
