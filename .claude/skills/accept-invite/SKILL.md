---
name: accept-invite
description: Handle Google Calendar auto-emails — four flows. (1) Invitation emails (subject begins with "Invitation:", body contains "You have been invited by"): accept the calendar event and archive the email. For internal/colleague invites of all types — 1:1s, team meetings, standups, syncs, demos, presentations, reviews, planning meetings. EXCEPT office-hours-style meetings (event title or body explicitly mentions "office hours", "office hour", or describes a drop-in / optional-attendance format with no specific agenda for the user) — those are archived without RSVPing because attendance is not expected. (2) RSVP-acceptance notifications the user receives when someone they invited accepts their meeting (subject begins with "Accepted:" and the body is the standard auto-generated RSVP template — attendee name, response status, event link, no human-written commentary): archive the email; the calendar is already updated and no action is needed. (3) Cancellation notifications from Google Calendar (subject begins with "Canceled:" or "Cancelled:", body is the standard auto-generated cancellation template — event title, time, organizer, "has been canceled", no human-written commentary): archive the email; Google Calendar has already removed the event from the user's calendar. SKIP the skill entirely when the sender is clearly external (an unknown company or vendor), when an acceptance or cancellation email contains a substantive note the user should read (e.g. the organizer added "sorry — rescheduling, new invite coming" or any commentary beyond the auto-template), when the response is "Declined:" or "Tentative:" (those may require the user to reschedule), or when the email is not actually a Google Calendar auto-message.
auto-handle: true
auto-handle-kinds:
  - email_msg
allowed-tools:
  - mcp__claude_ai_Google_Calendar__list_events
  - mcp__claude_ai_Google_Calendar__respond_to_event
  - mcp__claude_ai_Gmail__get_thread
  - mcp__claude_ai_Gmail__unlabel_thread
---

# Handle calendar auto-email

You're being invoked on an email task generated by Google Calendar. Decide which of four flows applies, then run it.

**Office-hours flow** (archive without RSVPing). Triggers:
- Event title or body explicitly mentions "office hours" or "office hour" (case-insensitive).
- OR the description makes clear it's a drop-in / optional-attendance format with no specific agenda for the user (e.g. "ask me anything", "open to the team", "no agenda — bring questions").

In this flow, **skip the calendar accept entirely** — attendance is not expected and RSVPing yes would imply a commitment the user hasn't made.
1. Use `mcp__claude_ai_Gmail__unlabel_thread` with `threadId=<the email's thread_id from the task source>` and `labelIds=["INBOX"]` to archive. The tool's required argument is **`threadId` (camelCase)** — the task source's field is `thread_id` (snake_case), so rename when passing. Passing `thread_id` makes the MCP reject with a misleading "Invalid label" error.
2. Return ONE sentence: `Archived office-hours invite "<event title>" without RSVPing.`

**Acceptance-notification flow** (archive only). Trigger:
- Subject begins with `Accepted:` (a Google Calendar auto-notification that someone accepted a meeting the user organized).
- AND the body is the standard auto-generated RSVP template — attendee name, response status, event link, no substantive human-written commentary.

If the accepter added a real note (anything beyond "Yes" / "I'll be there" / a calendar-link line), **skip this flow** — let the user see it.

1. Use `mcp__claude_ai_Gmail__unlabel_thread` with `threadId=<the email's thread_id from the task source>` and `labelIds=["INBOX"]` to archive. The tool's required argument is **`threadId` (camelCase)** — the task source's field is `thread_id` (snake_case), so rename when passing. Passing `thread_id` makes the MCP reject with a misleading "Invalid label" error.
2. Return ONE sentence: `Archived RSVP-acceptance notification for "<event title>".`

**Cancellation flow** (archive only). Trigger:
- Subject begins with `Canceled:` or `Cancelled:` (Google Calendar's auto-notification that a meeting on the user's calendar has been cancelled).
- AND the body is the standard auto-generated cancellation template — event title, time, organizer, "has been canceled" / similar phrasing, no substantive human-written commentary.

Google Calendar has already removed the event from the user's calendar by the time this email arrives, so there's nothing to RSVP — just archive. If the organizer added a real note (anything beyond the auto-template — e.g. "sorry — rescheduling, new invite coming", "moving this to Thursday", apology / context the user should read), **skip this flow** and let the user see it.

1. Use `mcp__claude_ai_Gmail__unlabel_thread` with `threadId=<the email's thread_id from the task source>` and `labelIds=["INBOX"]` to archive. The tool's required argument is **`threadId` (camelCase)** — the task source's field is `thread_id` (snake_case), so rename when passing. Passing `thread_id` makes the MCP reject with a misleading "Invalid label" error.
2. Return ONE sentence: `Archived cancellation notification for "<event title>".`

**Default flow** (accept and archive). For every other invite type — 1:1s, team meetings, standups, syncs, demos, presentations, reviews, planning meetings:

1. **Accept the calendar event.**
   - From the email `subject` and `body`, identify the event title and approximate date/time.
   - Use `mcp__claude_ai_Google_Calendar__list_events` with a narrow time window (e.g. ±1 day around the event date) and `fullText` set to a substring of the event title to locate the `eventId`.
   - Call `mcp__claude_ai_Google_Calendar__respond_to_event` with that `eventId` and `responseStatus="accepted"`.

2. **Archive the email.**
   - Use `mcp__claude_ai_Gmail__unlabel_thread` with `threadId=<the email's thread_id from the task source>` and `labelIds=["INBOX"]`. The tool's required argument is **`threadId` (camelCase)** — the task source's field is `thread_id` (snake_case), so rename when passing.

Return ONE sentence: `Accepted "<event title>" and archived the email.`

Don't ask for confirmation. If you can't find the calendar event in the default flow (e.g. the event is too far in the past/future to locate), say so in one sentence and skip the archive — don't guess. The office-hours and acceptance-notification flows don't need to find the event since they never RSVP.

This skill runs in two modes:
- **ACT+PTT (voice):** the user is holding the active task and has spoken an instruction. Their words are the trigger.
- **Auto-handle (screener):** no user instruction; you're invoked because the screener classifier picked this skill. Apply the same logic — the task source contains everything you need.
