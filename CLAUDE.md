# code-trip

STT -> LLM -> TTS orchestrator for coding on the go.

## Conventions

- All work is done on the `main` branch (single developer, no feature branches)
- Python package uses `src/` layout
- Tests live in `tests/` at the repo root
